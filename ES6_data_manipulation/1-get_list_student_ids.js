export default function getListStudentIds(studentIds){
    let studentIds = new Map 
    studentIds.set(id,1);
    studentIds.set(id,1);
    studentIds.set(id,1);
    let myarray = [studentIds]
    return my_array
}