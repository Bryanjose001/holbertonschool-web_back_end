export default function getListStudentIds(){
    let studentIds = new Map();
    studentIds.set(id,1);
    studentIds.set(id,2);
    studentIds.set(id,5);
    let myarray = [studentIds]
    return my_array
}